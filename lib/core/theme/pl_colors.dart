import 'package:flutter/material.dart';

class PlColors {
  // AppBar and Button Color (Red)
  static const Color primaryColor = Color(0xFFC40000);

  // Border Color for TextFormFields
  static const Color secundaryColor = Color(0xFF424242);

  // Background Color for Scaffold
  static const Color backgroundColor = Color(0xFFF5F5F5);

  // Focused Color for TextFormFields
  static const Color focusedColor = Color.fromARGB(255, 24, 24, 24);

  // Error Color
  static const Color errorColor = Color.fromARGB(255, 207, 35, 23);
}
